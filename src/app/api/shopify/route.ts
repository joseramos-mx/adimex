/**
 * Shopify API route — Phase 2 placeholder
 * This will proxy requests to the Shopify Storefront API.
 */

export async function GET() {
  return Response.json(
    { message: 'Shopify integration coming in Phase 2.' },
    { status: 503 }
  );
}
